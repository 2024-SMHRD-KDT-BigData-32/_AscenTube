# IDE & OS specific files
.idea/                # IntelliJ IDEA
.vscode/              # VS Code
*.iml                 # IntelliJ IDEA module files
.DS_Store             # macOS

# STS/Eclipse Workspace metadata (중요!)
/.metadata/           # 루트 워크스페이스 메타데이터
**/.metadata/         # 모든 하위 디렉토리의 메타데이터

# Node modules (프론트엔드 및 공통 JS/TS 라이브러리)
/front_end/node_modules/
/common-libs/node_modules/ # 만약 common-libs 폴더가 있고 node_modules가 있다면 추가

# Spring Boot build artifacts & generated files
/back_end/target/     # Spring Boot 빌드 결과물 (jar, war 포함)
*.jar                 # 혹시 모를 다른 위치의 jar 파일
*.war                 # 혹시 모를 다른 위치의 war 파일
/back_end/bin/        # IDE가 생성하는 컴파일된 클래스
/back_end/.gradle/    # Gradle 빌드 캐시
/back_end/.factorypath
/back_end/.sts4-cache/
/back_end/.classpath
/back_end/.project
/back_end/.settings/
/back_end/tokens/     # Google OAuth 토큰 등 민감 정보가 저장되는 폴더

# Maven Wrapper 파일 (필요시 추가, 권한 문제나 LF/CRLF 경고가 계속되면)
# /back_end/mvnw
# /back_end/mvnw.cmd

# 로그 파일
*.log
/.metadata/
